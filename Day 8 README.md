Utilizes .isupper() and .islower() methods to distinguish uppercase and lowercase letters in a string. 
Uses count function to count them and return the amount of upper and lowercase letters in the string
