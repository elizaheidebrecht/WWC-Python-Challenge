#Write a program to find the sum of all elements in a list.

#User enters list of numbers
input_list = float(input("Enter a list of number:"))
count_list = count.input_list
sum_of_list = sum(count.input_list)

if item in input_list:
    count[0:-1]


print(sum_of_list)
